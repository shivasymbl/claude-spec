# Dependabot Auto-Merge
# Automatically approves and merges Dependabot PRs after CI passes
#
# Auto-merge workflow for Asymbl Inc
# Requires repository setting "Allow auto-merge" to be enabled

name: Dependabot Auto-Merge

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    uses: asymblinc/.github/.github/workflows/reusable-dependabot-automerge.yml@main
    secrets: inherit
